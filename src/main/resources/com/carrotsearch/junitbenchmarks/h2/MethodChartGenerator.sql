
SELECT 
  NAME, 
  ROUND(ROUND_AVG, 4), 
  ROUND(ROUND_STDDEV, 4),
  ROUND(GC_AVG, 4),
  ROUND(GC_STDDEV, 4),
  GC_INVOCATIONS
FROM TESTS T, RUNS R
WHERE R.ID = ?
  AND T.RUN_ID = R.ID
  AND CLASSNAME = ?
